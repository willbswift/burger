<h1>Eat-Da-Burger!</h1>

<h2>Created Burgers</h2>

<ul class="readyToEat">
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have been eaten!</h2>

<ul class="devoured">
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Make a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" burger_name="burger_name">
  </div>

  <div class="form-group">
    <input type="radio" class="nodisplay" burger_name="devoured" value="0" checked><br>
    {{!-- <input type="radio" class="nodisplay"burger_name="devoured" value="1"> Not Eaten --}}
  </div>

  <button type="submit">Add Burger</button>
</form>
